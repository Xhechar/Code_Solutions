import { ConvertStackPipe } from './convert-stack.pipe';

describe('ConvertStackPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStackPipe();
    expect(pipe).toBeTruthy();
  });
});
